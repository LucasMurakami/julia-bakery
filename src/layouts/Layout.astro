---
import '../styles/globals.css';
import  SpeedInsights  from "@vercel/speed-insights/astro"
import Analytics from '@vercel/analytics/astro'
---

<!doctype html>
<html lang="pt-BR" class="h-full w-full lenis lenis-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta id="color-scheme-meta" name="color-scheme" content="light" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			rel="preload"
			as="style"
			href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
		/>
		<link
			rel="stylesheet"
			href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
			media="print"
			onload="this.media='all'"
		/>
		<noscript>
			<link
				rel="stylesheet"
				href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
			/>
		</noscript>
		<meta name="generator" content={Astro.generator} />
		<title>Pinckmax Bakery – O Melhor Caseiro</title>
		<meta
			name="description"
			content="Um affair de amor com a confeitaria. Cookies, bolos e doces caseiros. Tome um café ou peça online. St-Henri, Montreal."
		/>
		<script is:inline>
			const getThemePreference = () => {
				if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
					return localStorage.getItem('theme');
				}
				return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
			};
			const isDark = getThemePreference() === 'dark';
			document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
			const meta = document.getElementById('color-scheme-meta');
			if (meta) meta.setAttribute('content', isDark ? 'dark' : 'light');
		</script>
	</head>
	<body class="m-0 min-h-full w-full overflow-x-hidden">
		<slot />
		<SpeedInsights />
		<Analytics />
		<script>
			import { initSmoothScroll, initScrollAnimations } from '@/scripts/smoothScroll';
			document.addEventListener('DOMContentLoaded', () => {
				initSmoothScroll();
				initScrollAnimations();
				setTimeout(initScrollAnimations, 150);
			});
		</script>
	</body>
</html>
